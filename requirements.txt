# =============================================================================
# Vulnerability Index Pipeline — Python Dependencies
# =============================================================================
# Thesis: Building Vulnerability Assessment, Nepal Lumbini Province
# Pipeline version: v27
#
# Install with:
#   pip install -r requirements.txt
#
# For a reproducible environment, install into a dedicated virtual environment
# or conda environment (see SETUP.md for full instructions).
#
# Version pins reflect the environment used during pipeline development.
# Minimum versions are indicated where the pin is known to be non-strict;
# exact pins are recommended for full reproducibility (FAIR principle).
# =============================================================================


# ── Core data manipulation ───────────────────────────────────────────────────
numpy>=1.24,<3.0          # Array operations; numexpr acceleration optional
pandas>=2.0,<4.0          # DataFrame I/O, groupby, scoring logic
numexpr>=2.8              # Optional: pandas performance on large DataFrames
                          # (detected automatically by pandas)

# ── Visualisation ────────────────────────────────────────────────────────────
matplotlib>=3.7,<4.0      # All plot outputs (heatmaps, scatter, bar, P-P)
seaborn>=0.12,<1.0        # Heatmaps in diagnostic stages (03, 04)
Pillow>=9.0               # OpenStreetMap tile rendering (osm_tiles.py)

# ── Statistics and spatial analysis ─────────────────────────────────────────
scipy>=1.10               # Statistical tests: Spearman, Kruskal-Wallis,
                          # Mann-Whitney U, Little's MCAR, hierarchical
                          # clustering (stages 03, 04, 06, 12–22, 24)

# ── Machine learning ─────────────────────────────────────────────────────────
scikit-learn>=1.3         # KMeans, PCA, silhouette, KNNImputer,
                          # IterativeImputer (MICE), RandomForest,
                          # permutation importance, GridSearchCV
                          # (stages 05, 07–09, 23–24, 25–26)

# ── Missing data diagnostics ─────────────────────────────────────────────────
pyampute>=0.1             # Little's MCAR test (MCARTest)
                          # (stages 03, 03a, 03b)

# ── Generalised Additive Models ──────────────────────────────────────────────
pygam>=0.9                # LinearGAM with penalised splines (stages 25–26)
                          # Install: pip install pygam
                          # Note: pygam is not on conda-forge; use pip inside
                          # conda environments.

# ── Network / tile fetching ──────────────────────────────────────────────────
requests>=2.28            # HTTP fallback in osm_tiles.py for tile fetching
                          # (stages 14, 19, 20)
